// Public integration settings only. Never place service/API secrets here.
window.oriphimContact = {
  calendlyUrl: '',
  // Calendly custom text questions must match this order.
  calendlyQuestions: { institution: 'a1', research_area: 'a2', description: 'a3' },
  // A same-origin signup endpoint, to be connected to the chosen email service.
  signupEndpoint: ''
};
